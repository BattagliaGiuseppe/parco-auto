# P2.2.1 - Driver Assignment

Adds pilot assignment to reconciled connected sessions. The selected driver is stored directly on `event_car_turns.driver_id`, so Connected Vehicles and Events share the same source of truth.

Database migration is already live. Do not execute SQL manually.
