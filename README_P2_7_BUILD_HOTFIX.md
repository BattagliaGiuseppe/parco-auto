# P2.7 Build Hotfix

Correzione TypeScript per `app/driver-display/page.tsx`.

Fix:
- narrowing esplicito di `reference`, `engine` e `referenceStart` prima del callback `navigator.geolocation.watchPosition`;
- nessuna modifica al database;
- nessuna modifica agli endpoint;
- nessuna modifica funzionale alla logica P2.7.
